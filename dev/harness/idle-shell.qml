import Quickshell

// A Quickshell with nothing in it. The zero line for dev/perf.sh: whatever
// this costs is what the runtime costs, and a notch nobody is touching has to
// be indistinguishable from it.
ShellRoot {}
